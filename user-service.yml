server:
  port: 5001

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8002/eureka/

spring:
  data:
    mongodb:
      host: 52.172.197.84
      port: 27017
      database: auth-db
feign:
  hystrix:
    enabled: false
