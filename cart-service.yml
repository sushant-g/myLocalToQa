server:
  port: 9002

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8002/eureka/

spring:
  data:
    mongodb:
      host: 52.172.197.84
      port: 27017
      database: cart-db
feign:
  hystrix:
    enabled: false
